# 몰리 놀이터: 배틀 기획서

> 문서 상태: 초안  
> 최종 갱신: 2026-09-19  
> 장르: Discord 기반 무조작 텍스트 전투 시뮬레이터

## 1. 개요

몰리 놀이터의 배틀은 Discord 사용자가 자신의 마비노기 모바일 캐릭터를 등록하고, 다른 사용자의 캐릭터에게 도전하여 자동으로 전투하는 텍스트 기반 게임이다.

사용자가 하는 조작은 `/배틀` 명령으로 상대를 지정하는 것뿐이다. 전투가 시작된 뒤에는 공격·스킬·방어 등의 명령이나 버튼을 제공하지 않는다. 몰리가 두 캐릭터의 클래스, 능력치, 스킬을 바탕으로 모든 행동을 결정하고 전투 상황을 Discord 텍스트 메시지로만 중계한다.

```text
종잇장이 핏피에게 일반 공격!
치명타! 456의 피해를 입혔습니다!
핏피는 아직 끄떡없습니다!
```

### 1.1 핵심 한 줄

> 마비노기 모바일의 실제 캐릭터 정보와 클래스 스킬을 이용해 몰리가 진행하는 Discord용 텍스트 자동전투 게임.

### 1.2 용어

| 용어 | 의미 |
| --- | --- |
| 캐릭터 | Discord 사용자가 등록한 마비노기 모바일 캐릭터 |
| 도전자 | `/배틀` 명령을 실행한 사용자 |
| 상대 | 배틀 대상으로 지정된 Discord 사용자 |
| 게임 스레드 | `/배틀`을 실행한 일반 텍스트 채널에 만드는 전투 로그용 공개 스레드 |
| 전투 스냅샷 | 전투 시작 시 고정한 캐릭터 클래스와 능력치 |
| 전투 이벤트 | 공격, 피해, 치명타, 회복, 상태 이상 등 전투 중 발생한 사건 |

## 2. 기획 목표

- 기존 랭킹 조회 기능을 놀이 콘텐츠로 확장한다.
- 실제 캐릭터의 클래스와 능력치를 전투에 반영한다.
- Google Sheets에서 관리하는 클래스·스킬 데이터를 활용한다.
- 플레이어의 지속적인 조작 없이도 짧고 재미있는 전투를 제공한다.
- 일반 채팅을 방해하지 않는 독립적인 전투 공간을 사용한다.
- 전투 결과가 능력치만으로 완전히 고정되지 않도록 클래스와 스킬, 적절한 무작위성을 반영한다.
- 이후 전적, 토너먼트, 관전자 기능 등으로 확장할 수 있는 구조를 만든다.

## 3. 초기 범위에서 제외할 것

다음 기능은 초기 버전에 포함하지 않는다.

- 전투 중 플레이어의 실시간 스킬 선택
- 관전자의 전투 개입
- 장비 등록 및 장비 강화
- 별도의 캐릭터 레벨업과 성장 재화
- 포인트 상점과 유료성 재화
- 시즌 보상
- 토너먼트
- AI를 이용한 실시간 전투 문장 생성
- 생활력과 매력의 복잡한 전투 시스템

## 4. 사용자 흐름

### 4.1 캐릭터 등록

예상 명령 형식:

```text
/캐릭터등록 서버:칼릭스 캐릭터이름:종잇장
```

진행 과정:

1. 사용자가 서버와 캐릭터 이름을 입력한다.
2. 몰리가 마비노기 모바일 랭킹에서 캐릭터를 검색한다.
3. 검색 결과가 없거나 여러 결과를 구분할 수 없으면 등록하지 않는다.
4. 검색 결과를 사용자에게 보여주고 등록 여부를 확인한다.
5. 확인하면 Discord 사용자와 캐릭터를 연결하여 DB에 저장한다.
6. 기존에 등록한 캐릭터가 있다면 변경 여부를 확인한다.

등록 정책:

- Discord 사용자 한 명은 캐릭터를 하나만 등록할 수 있다.
- 캐릭터 등록 정보는 길드별로 나누지 않고 봇 전체에서 공통으로 사용한다.
- 동일한 게임 캐릭터를 여러 Discord 사용자가 등록하는 것은 최종적으로 금지한다.
- 다만 캐릭터 소유권을 봇이 검증할 수 없으므로 중복 등록 방지 기능은 후순위로 구현한다.
- 봇은 서버 이전이나 캐릭터명 변경을 자동으로 추적하지 않는다.
- 동일한 서버와 캐릭터명으로 조회되는 클래스와 능력치 변경은 랭킹 갱신 시 반영한다.

등록 시 확보할 후보 데이터:

- Discord 사용자 ID
- Discord 서버 ID
- 마비노기 모바일 서버
- 캐릭터 이름
- 현재 클래스
- 전투력
- 생활력
- 매력
- 각 랭킹 및 순위
- 마지막 조회 시각

### 4.2 캐릭터 조회 및 갱신

필요할 가능성이 높은 보조 명령:

```text
/내캐릭터
/캐릭터갱신
/캐릭터변경
/캐릭터해제
```

랭킹 데이터의 유효 시간은 **1시간**으로 한다.

- 마지막 정상 갱신 후 1시간 이내이면 DB에 저장된 정보를 사용한다.
- 1시간이 지났다면 배틀 시작 직전에 랭킹을 다시 검색한다.
- 검색에 성공하면 클래스와 능력치를 갱신한 뒤 배틀을 시작한다.
- 도전자와 상대 중 한 명이라도 랭킹에서 검색되지 않으면 배틀을 시작하지 않는다.
- 검색 실패 시 등록 정보는 즉시 삭제하지 않는다. 일시적인 외부 서비스 장애일 수 있기 때문이다.
- 외부 서비스 장애와 실제 검색 결과 없음의 세부 처리 차이는 추후 결정한다.

### 4.3 배틀 신청

예상 명령 형식:

```text
/배틀 상대:@핏피
```

기본 흐름:

1. 명령을 실행한 채널이 배틀을 허용한 채널인지 확인한다.
2. 현재 길드에서 다른 게임이 진행 중인지 확인한다.
3. 상대가 현재 같은 Discord 길드에 속해 있는지 확인한다.
4. 도전자와 상대가 모두 캐릭터를 등록했는지 확인한다.
5. 마지막 갱신 시각이 1시간을 넘긴 캐릭터는 랭킹에서 다시 검색한다.
6. 두 캐릭터가 모두 유효하면 상대방의 수락 없이 명령 실행 채널에 게임 스레드를 생성한다.
7. 두 캐릭터의 전투 시작 시점 능력치를 스냅샷으로 저장한다.
8. 몰리가 자동전투를 진행한다.
9. 전투 결과와 주요 이벤트를 저장한다.
10. 결과 요약을 출력한다.
11. 게임 스레드는 다른 게임과 같이 종료 후 보관한다.

예외 처리 후보:

- 자기 자신에게 도전
- 등록하지 않은 사용자에게 도전
- 같은 길드에 속하지 않은 사용자에게 도전
- 길드에서 이미 다른 게임이 진행 중
- 외부 랭킹 조회 실패
- 클래스 또는 스킬 데이터가 없음
- 배틀 도중 봇 재시작

## 5. 전투 공간

전투는 명령을 실행한 채널의 게임 스레드에서 진행한다.

- `/배틀`은 서버의 일반 텍스트 채널에서 실행한다. 기존 스레드 안에서는 시작하지 않는다.
- 전투 공간은 명령 실행 채널에 공개 스레드로 만든다.
- 스레드 이름은 `배틀-날짜시간` 형식을 사용한다.
- 전투 종료 뒤 짧은 결과 확인 시간을 두고 스레드를 읽기 전용으로 잠그고 보관한다.
- 스레드를 만들 수 없으면 대체 채널을 사용하지 않고 실패 사실을 알린다.
- 한 길드에서는 게임 하나만 동시에 진행할 수 있다. 배틀과 다른 게임도 같은 길드 단위 잠금을 공유한다.
- 전투가 정상 종료되거나 오류로 중단되면 길드의 게임 잠금을 반드시 해제한다.

## 6. 전투 기본 구조

### 6.1 기본 원칙

- 전투 시작 후 플레이어와 관전자의 입력을 받지 않고 완전히 자동으로 진행한다.
- 이미지, 애니메이션, 전투 화면을 사용하지 않고 Discord 텍스트 메시지만 출력한다.
- 서버에서는 첫 로그를 보내기 전에 전투 전체를 시뮬레이션하여 승패와 이벤트를 확정한다.
- 확정된 이벤트를 일정한 시간 간격으로 전송하여 실시간 전투처럼 중계한다.
- 한 행동은 하나 이상의 전투 이벤트를 발생시킬 수 있다.
- 행동 선택은 클래스별 조건식 AI와 스킬 쿨다운을 기반으로 한다.
- 모든 확률 판정에는 해당 배틀의 난수 시드를 사용한다.
- 선공을 결정한 뒤 두 캐릭터가 교대로 행동한다.
- 기본 공격은 항상 명중하고, 회피와 빗나감은 스킬 또는 상태 효과로만 발생한다.
- 한 판은 짧게 끝나야 하며 최대 주요 행동 수를 넘기면 남은 HP 비율로 판정한다.

### 6.2 목표 플레이 시간

| 항목 | 초기 목표 |
| --- | --- |
| 전투 시간 | 약 15~25초 |
| 평균 주요 행동 | 8~12회 |
| 로그 출력 간격 | 약 1.5초 |
| 최대 주요 행동 | 16회 |
| 길드당 동시 놀이터 | 1개 |

반격, 지속 피해, 상태 변화 등은 주요 행동에 딸린 부가 이벤트로 취급하며 최대 주요 행동 수에는 포함하지 않는다.

### 6.3 전투 순서

1. 캐릭터 스냅샷 생성
2. 전투용 능력치 계산
3. 선공 결정
4. 행동 캐릭터의 사용 가능한 스킬 확인
5. 조건과 우선순위, 가중치를 이용한 행동 선택
6. 피해 편차와 치명타 등 확률 판정
7. 스킬 효과 적용
8. 전투 이벤트 생성
9. 이벤트에 맞는 메시지 출력
10. 전투 종료 조건 확인
11. 행동권을 상대에게 넘기고 다음 행동 진행
12. 최대 주요 행동에 도달하면 남은 HP 비율로 판정
13. 결과와 주요 이벤트 저장
14. 확정된 이벤트를 텍스트 메시지로 순차 출력

### 6.4 기본 확률 규칙

| 항목 | 확정된 기본값 |
| --- | --- |
| 일반 공격 명중 | 100% |
| 피해량 편차 | 계산된 기본 피해의 90~110% |
| 기본 치명타 확률 | 10% |
| 치명타 피해 배율 | 150% |
| 일반 회피 | 없음 |

클래스 패시브와 스킬은 치명타 확률과 피해 배율을 변경하거나 회피를 발생시킬 수 있다. 모든 확률과 피해 편차는 난수 시드를 이용해 재현 가능해야 한다.

## 7. 능력치 설계 방향

### 7.1 전투력

전투력은 전투의 중심 능력치로 사용한다. 다만 실제 전투력을 공격력이나 체력에 그대로 대입하면 전투력 차이가 곧 확정 승패가 될 수 있으므로 정규화가 필요하다.

가능한 활용:

- 최대 체력
- 기본 공격력
- 방어력
- 전투력 차이에 따른 소폭 보정

결정이 필요한 사항:

- 전투력을 전투용 수치로 변환하는 공식
- 전투력 차이가 승률에 미치는 최대 영향
- 매우 큰 전투력 격차에서도 역전 가능성을 둘지 여부

### 7.2 생활력

생활력은 부가적인 전투 요소로 사용할 예정이다. 구체적인 역할은 미정이다.

후보:

- 최대 체력 소폭 증가
- 턴마다 체력 회복
- 회복 스킬 효율 증가
- 상태 이상 저항
- 장기전 보너스

### 7.3 매력

매력도 부가적인 전투 요소로 사용할 예정이다. 구체적인 역할은 미정이다.

후보:

- 선공 확률
- 치명타 또는 회피 확률의 소폭 보정
- 유리한 특수 이벤트 발생
- 상대 행동 실패
- 관중 응원 효과
- 특별 전투 대사 출력

생활력과 매력을 합친 영향은 전투력과 클래스·스킬보다 작게 유지하는 방향을 우선 검토한다.

## 8. 클래스와 스킬

### 8.1 데이터 출처

- 클래스와 스킬 데이터: Google Sheets의 `디스코드봇_몰리_테이블`
- 현재 사용 시트: `클래스`, `스킬`, `배틀스킬`, `배틀스킬효과`, `배틀스킬파생`, `배틀자원`
- 캐릭터 능력치: 랭킹 검색 결과와 DB에 저장된 마지막 정상 데이터

Google Sheets 주소:

- <https://docs.google.com/spreadsheets/d/19kRuVIlZ1LEhU5lixEjRqYvZKbdGnXk0tjY6qznSRf0/edit>

Google Sheets는 기준 데이터의 원본으로 사용하고, 전투 시작 시마다 원격 조회하지 않는다. 갱신된 데이터를 검증한 뒤 봇 내부 캐시 또는 로컬 데이터로 교체하고, 실패하면 마지막 정상 데이터를 유지한다.

### 8.2 데이터 분리 원칙

마비노기 모바일의 원본 정보와 몰리 배틀의 규칙을 같은 시트에 섞지 않는다.

| 구분 | 시트 | 책임 |
| --- | --- | --- |
| 원본 데이터 | `클래스` | 클래스 설명과 기본 장착 스킬 구성 |
| 원본 데이터 | `스킬` | 마비노기 모바일의 스킬명, 공식 설명, 태그와 파생 관계 |
| 배틀 데이터 | `배틀스킬` | 자동전투에서 스킬을 선택하고 사용하는 규칙 |
| 배틀 데이터 | `배틀스킬효과` | 피해, 회복, 강화, 약화 등 스킬 실행 결과 |
| 배틀 데이터 | `배틀스킬파생` | 부모 스킬이 파생 스킬을 선택하거나 예약하는 규칙 |
| 배틀 데이터 | `배틀자원` | 클래스 전용 태세·게이지·중첩 자원의 공통 정의 |

`스킬.ID`와 `배틀스킬.ID`는 동일한 값을 사용한다. 원본에 존재하지만 몰리 배틀에서 아직 구현하지 않은 스킬은 `스킬`에만 두거나 `배틀스킬.활성화`를 `FALSE`로 둔다.

스킬 시트에는 `클래스ID`를 두지 않는다. 클래스에서 기본 장착 스킬 ID를 참조하고, 파생 스킬은 `스킬.부모스킬ID`를 따라 탐색한다.

#### 배틀 시트 변경 권한과 관리 원칙

`클래스`와 `스킬`은 마비노기 모바일의 원본 정보를 보존하는 안정 영역으로 취급한다. 반면 이름이 `배틀`로 시작하는 시트는 몰리 배틀 엔진의 구현 데이터이므로, 앞으로의 기획과 개발에 필요하면 GPT와 Codex가 필드를 추가·변경·삭제하고 시트를 분리·통합할 수 있다.

변경할 때는 다음 원칙을 지킨다.

- 변경 전에 최신 시트 구조와 데이터를 읽고 기존 행을 보존한다.
- 기획서의 시트 명세, 로딩 순서, 검증 규칙도 같은 작업에서 함께 갱신한다.
- 필드 삭제·이름 변경·의미 변경은 로더 호환성에 영향을 주므로 마이그레이션 사항을 문서에 남긴다.
- 임시 자유문자열보다 명시적인 ID, 조건, 연산자와 참조 방식을 우선한다.
- 특정 클래스만을 위한 코드 예외보다 데이터로 표현할 수 있는 공통 구조를 우선한다.
- 시트 구조 변경 후 헤더, 참조 무결성, 필수 값과 기존 데이터 보존 여부를 다시 확인한다.

```text
클래스
 ├─ 스킬1~스킬5
 └─ 궁극기
      ↓ ID 참조
스킬 ── 동일 ID ── 배틀스킬
                       ├─ 배틀스킬효과
                       ├─ 배틀스킬파생 ── 파생 스킬
                       └─ 배틀자원 ── 태세·게이지·중첩
```

### 8.3 공통 ID 규칙

- ID는 영문 소문자 `snake_case`를 사용한다.
- 화면에 표시할 한글 이름을 키로 사용하지 않는다.
- 한 번 배포한 ID는 표시 이름이 바뀌어도 변경하지 않는다.
- 클래스 ID와 스킬 ID는 각각의 시트 안에서 유일해야 한다.
- 효과 ID는 `{스킬ID}_{두 자리 순번}` 형식을 기본으로 한다.
- 파생 규칙 ID는 `{부모스킬ID}_{파생 의미}` 형식을 기본으로 한다.
- `클래스.스킬1~궁극기`, `스킬.부모스킬ID`, `배틀스킬효과.스킬ID`, `배틀스킬파생.부모스킬ID`, `배틀스킬파생.파생스킬ID`는 모두 존재하는 ID만 참조해야 한다.

예시:

```text
클래스: bard
기본 스킬: bards_tale
파생 스킬: heroic_hymn
효과: heroic_hymn_01
파생 규칙: bards_tale_heroic
```

### 8.4 `클래스` 시트

클래스의 원본 설명과 기본 장착 스킬 세트를 정의한다.

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `ID` | 문자열 | O | 클래스 고유 ID |
| `이름` | 문자열 | O | 사용자에게 표시할 클래스명 |
| `계열` | 문자열 | O | 전사, 궁수, 마법사, 힐러, 음유시인, 도적 계열 |
| `설명` | 문자열 | O | 마비노기 모바일 공식 클래스 설명 |
| `스킬1` | 스킬 ID | 클래스 구현 시 O | 첫 번째 기본 스킬 |
| `스킬2` | 스킬 ID | 클래스 구현 시 O | 두 번째 기본 스킬 |
| `스킬3` | 스킬 ID | 클래스 구현 시 O | 세 번째 기본 스킬 |
| `스킬4` | 스킬 ID | 클래스 구현 시 O | 네 번째 기본 스킬 |
| `스킬5` | 스킬 ID | 클래스 구현 시 O | 다섯 번째 기본 스킬 |
| `궁극기` | 스킬 ID | 클래스 구현 시 O | 궁극기 스킬 |

현재 음유시인 구성:

| 필드 | 스킬 ID | 표시 이름 |
| --- | --- | --- |
| `스킬1` | `string_shot` | 스트링 샷 |
| `스킬2` | `melody_shock` | 멜로디 쇼크 |
| `스킬3` | `triple_stroke` | 트리플 스트로크 |
| `스킬4` | `bards_tale` | 바즈 테일 |
| `스킬5` | `feign_death` | 죽은 척 하기 |
| `궁극기` | `battle_symphony` | 배틀 심포니 |

`클래스`에는 파생 스킬을 직접 나열하지 않는다.

### 8.5 `스킬` 시트

마비노기 모바일에서 확인한 원본 스킬 정보를 보존한다. 피해 계수, 쿨다운 턴, AI 우선순위 등 몰리 배틀 전용 수치는 기록하지 않는다.

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| `ID` | 문자열 | O | 원본 및 배틀에서 공통으로 사용하는 스킬 ID |
| `이름` | 문자열 | O | 공식 스킬명 |
| `스킬구분` | 문자열 | O | `일반`, `궁극기`, `파생` 중 하나 |
| `부모스킬ID` | 스킬 ID | 파생만 O | 파생 스킬의 부모. 일반·궁극기는 빈 값 |
| `설명` | 문자열 | O | 게임에서 확인한 공식 설명 |
| `태그` | 문자열 | 선택 | 공식 태그를 쉼표로 구분하여 저장 |

음유시인 원본 데이터는 기본 스킬과 궁극기 6개, 파생 스킬 5개로 총 11개다.

```text
bards_tale
├─ heroic_hymn
├─ indomitable_overture
└─ song_of_hope

feign_death
├─ rising_windmill
└─ fine_tuning
```

### 8.6 `배틀스킬` 시트

자동전투에서 스킬을 후보로 만들고 선택하며 실행하기 위한 규칙을 정의한다. `ID`는 반드시 같은 원본 스킬의 `스킬.ID`와 일치해야 한다.

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `ID` | 스킬 ID | `스킬.ID`와 동일한 고유 ID |
| `활성화` | 불리언 | `FALSE`이면 로드하되 행동 후보에서는 제외 |
| `행동분류` | 문자열 | 공격, 회복, 지원, 방해, 생존, 반격, 궁극기 등의 조합 |
| `대상유형` | 문자열 | 자신, 상대, 자신·상대 등 대상 선택 방식 |
| `기본쿨다운` | 정수 | 사용 후 다시 후보가 되기까지 필요한 주요 행동 수 |
| `최초쿨다운` | 정수 | 전투 시작 시 적용할 초기 쿨다운 |
| `사용우선순위` | 정수 | 조건을 만족한 후보끼리 비교할 우선순위 |
| `사용조건` | 문자열 | 현재는 사람이 읽는 기획 값. 구현 전에 조건 타입으로 정규화 필요 |
| `자원유형` | 문자열 | 악상, 궁극기 게이지 등 사용하는 전용 자원 |
| `자원소모` | 숫자 | 사용 시 소모하는 자원 |
| `자원획득` | 숫자 | 사용 시 얻는 자원 |
| `궁극기여부` | 불리언 | 궁극기 전용 처리 여부 |
| `배틀설명` | 문자열 | 몰리 배틀에서 단순화한 동작 설명 |
| `사용문구` | 문자열 | 기본 전투 출력 템플릿 |
| `비고` | 문자열 | 원본과 다른 단순화 및 임시 결정 기록 |

`사용우선순위`가 높다고 항상 선택되는 것은 아니다. 먼저 쿨다운, 자원, 상태와 사용 조건을 검사하고, 남은 후보 사이에서 우선순위와 추후 확정할 가중치 규칙을 사용한다.

파생 전용 스킬은 독립 행동 후보로 선택하지 않는다. 현재 데이터에서는 기본쿨다운과 우선순위를 `0`으로 두고 `배틀스킬파생`을 통해서만 실행한다.

### 8.7 `배틀스킬효과` 시트

스킬 한 번이 발생시키는 결과를 효과 단위의 여러 행으로 정의한다.

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `ID` | 문자열 | 효과 고유 ID |
| `스킬ID` | 스킬 ID | 효과를 소유한 `배틀스킬.ID` |
| `실행순서` | 정수 | 동일 스킬 안에서 효과를 처리할 순서 |
| `효과유형` | 문자열 | 피해, 회복, 지속피해, 상태효과, 쿨다운감소 등 |
| `대상` | 문자열 | 자신 또는 상대 |
| `계수기준` | 문자열 | 현재는 `공격력` 또는 빈 값 |
| `계수` | 실수 | 기준 능력치에 곱할 값. `0.67`은 공격력의 67% |
| `고정값` | 숫자 | 계수 계산과 별개로 사용하는 고정값 |
| `횟수` | 정수 | 동일 효과 반복 횟수 |
| `발동확률` | 실수 | `0.0~1.0`. `1`은 100% |
| `지속턴` | 정수 | 상태 및 지속 효과가 유지되는 턴 수 |
| `상태효과ID` | 문자열 | 적용하거나 검사할 상태 효과 ID |
| `최대중첩` | 정수 | 같은 상태 효과의 최대 중첩 수 |
| `효과문구` | 문자열 | 효과 발생 시 출력할 문구 템플릿 |
| `비고` | 문자열 | 원본 수치와 단순화 근거 |

효과를 `배틀스킬`과 분리하는 이유는 한 스킬이 서로 다른 대상과 조건을 가진 여러 결과를 순서대로 발생시키기 때문이다.

예를 들어 `희망의 송가`는 한 번의 사용으로 다음 효과들을 만든다.

```text
1. 상대에게 5회 피해
2. 자신 회복
3. 상대에게 현기증
4. 자신의 공격 속도 증가
5. 자신이 주는 피해 증가
6. 자신이 받는 피해 감소
```

이를 `효과1`, `효과2`처럼 가로 열에 넣으면 새 효과가 생길 때마다 스키마와 로더를 변경해야 한다. 별도 행으로 두면 효과 수가 달라도 동일한 로더가 `스킬ID`로 수집한 뒤 `실행순서`대로 처리할 수 있다.

피해와 회복의 기본 계산식 초안:

```text
단일 결과 = 계수기준 능력치 × 계수 + 고정값
전체 결과 = 단일 결과 × 횟수
```

각 반복 효과가 개별 치명타·방어·확률 판정을 수행할지는 효과 유형과 추후 전투 규칙에서 결정한다. 구현 시 단순히 전체 결과에 치명타를 한 번 적용하지 않도록 주의한다.

### 8.8 `배틀스킬파생` 시트

원본 파생 관계는 `스킬.부모스킬ID`에 보존하고, 이 시트에는 몰리 배틀에서 실제로 어떤 조건과 순서로 파생 스킬을 실행할지 기록한다.

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `ID` | 문자열 | 파생 규칙 고유 ID |
| `부모스킬ID` | 스킬 ID | 파생을 시작하는 스킬 |
| `파생스킬ID` | 스킬 ID | 실행할 파생 스킬 |
| `발동방식` | 문자열 | `무작위`, `조건` 등 선택 방식 |
| `가중치` | 숫자 | 무작위 선택 시 상대 가중치 |
| `발동확률` | 실수 | 조건 성립 후 실제 발동 확률 |
| `조건유형` | 문자열 | 악상, 피격·다운, 생존 등 검사 대상 |
| `조건값` | 문자열 | 조건을 만족시키는 값 |
| `중복허용` | 불리언 | 같은 파생 스킬의 연속 선택 허용 여부 |
| `실행시점` | 문자열 | 즉시, 반격 시, 다음 행동 등 |
| `비고` | 문자열 | 임시 규칙과 원본 차이 |

현재 음유시인의 배틀 파생 초안:

- `바즈 테일`: 용맹의 찬가, 불굴의 서곡, 희망의 송가 중 하나를 동일 가중치로 즉시 선택한다.
- `죽은 척 하기`: 죽은 척 중 공격받거나 다운되면 라이징 윈드밀로 반격한다.
- `죽은 척 하기`: 공격받지 않고 종료되면 다음 행동으로 파인 튜닝을 사용한다.

`발동방식`, `조건유형`, `실행시점`은 현재 한글 문자열이다. 개발을 시작할 때 코드의 enum 또는 명시적인 규칙 타입으로 매핑하고, 알 수 없는 값은 무시하지 말고 전체 데이터 검증을 실패시켜야 한다.

### 8.9 `배틀자원` 시트

클래스가 사용하는 태세, 게이지와 중첩을 공통 형식으로 정의한다. 스킬 효과는 자원 ID를 참조하여 생성·소모·조건 검사·수치 계산을 수행한다.

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `ID` | 문자열 | 자원 고유 ID |
| `이름` | 문자열 | 사용자와 기획자에게 표시할 이름 |
| `분류` | 문자열 | 태세, 자원, 중첩 등 처리 방식 |
| `최대값` | 숫자 | 최대 보유량. 미확정 값은 `0`으로 두고 활성화 전에 확정 |
| `초기값` | 숫자 | 전투 시작 시 보유량 |
| `지속턴` | 정수 | 자동 소멸까지의 턴. 전투 동안 유지하면 `0` |
| `중첩방식` | 문자열 | 가산, 교체, 상호배타 등 |
| `전투종료시제거` | 불리언 | 전투 스냅샷 종료 시 제거 여부 |
| `설명` | 문자열 | 동작과 제약 설명 |

현재 댄서 확장용 자원은 `dance_grace`(우아), `dance_passion`(정열), `rhythm`(리듬), `tempo`(템포), `performance_heat`(공연의 열기)다. 우아와 정열은 상호배타 태세이며, 아직 캡처만으로 확정할 수 없는 최대값은 0으로 표시한다.

### 8.10 댄서 스킬 모델링 기준

댄서는 음유시인의 단순 피해·상태 구조에 더해 태세, 조건부 자원 생성, 기본 공격 변경, 브레이크, 다중 파생과 자원 중첩 참조가 필요하다.

- `배틀스킬효과`에 `조건대상`, `조건유형`, `조건ID`, `조건연산자`, `조건값`을 추가한다.
- 다른 자원의 현재값이나 소모량으로 효과 수치를 계산하기 위해 `수치참조ID`, `수치참조방식`을 추가한다.
- 효과 유형은 필요에 따라 `자원증가`, `자원소모`, `상태전환`, `기본공격변경`, `브레이크피해`, `브레이크면역` 등을 사용한다.
- `배틀스킬파생.우선순위`로 여러 파생 조건이 동시에 성립할 때 선택 순서를 결정한다. 숫자가 클수록 먼저 평가하는 것을 기본 규칙으로 한다.
- 애드리브의 우아·정열·화합 동작은 한 셀에 조건문으로 넣지 않고 부모 스킬과 파생 스킬로 나눈다. 화합 발동은 일반 태세 파생보다 높은 우선순위를 갖는다.
- 피날레의 첫 사용과 재사용 마무리는 부모·파생 스킬로 나누고, 마무리 피해는 소모한 `performance_heat` 중첩을 수치 참조한다.
- 이동 거리와 범위는 공간 좌표가 없는 초기 배틀에서는 원본 `스킬.설명`에 보존하되, 직접적인 배틀 수치로 사용하지 않는다.

### 8.11 음유시인 배틀 데이터 기준

음유시인 데이터는 공격력 `56,024`인 캐릭터의 스킬 화면 캡처를 기준으로 작성했다. 피해 및 회복 계수는 다음과 같이 역산한 추정값이다.

```text
추정 계수 = 화면 표시 수치 ÷ 56,024
```

예시:

| 스킬 | 표시 수치 | 저장 계수 |
| --- | ---: | ---: |
| 스트링 샷 | 37,536 | `0.670` |
| 트리플 스트로크 | 28,012 × 3 | `0.500`, 3회 |
| 용맹의 찬가 | 16,022 × 5 | `0.286`, 5회 |
| 라이징 윈드밀 | 14,263 × 4 | `0.255`, 4회 |
| 파인 튜닝 마지막 타격 | 67,564 | `1.206` |
| 배틀 심포니 타격 | 50,298 | `0.898` |

이 계수는 원본 게임 공식을 확정한 값이 아니다. 장비, 버프, 반올림, 숨겨진 고정값이 포함되었을 수 있으므로 몰리 배틀의 초기 밸런스 초안으로만 사용한다.

초 단위 원본 효과는 현재 턴 단위로 임시 변환했다.

| 원본 시간 | 현재 변환 |
| --- | --- |
| 현기증 9초 | 3턴 |
| 약점 노출 15초 | 4턴 |
| 죽은 척 4초 | 1턴 |
| 파인 튜닝 강화 30초 | 6턴 |
| 배틀 심포니 쿨다운 감소 2초 × 5 | 모든 스킬 2턴 감소로 단순화 |

시간 변환과 배틀 계수는 확정 규칙이 아니다. 실제 시뮬레이션에서 평균 주요 행동 수와 승률을 측정한 뒤 변경한다.

### 8.12 데이터 로딩과 검증

Google Sheets 전체를 하나의 버전으로 취급한다. 일부 시트만 성공한 상태로 캐시를 교체하지 않는다.

권장 로딩 순서:

1. `클래스`를 로드하고 클래스 ID의 유일성을 검사한다.
2. `스킬`을 로드하고 스킬 ID와 부모 관계를 검사한다.
3. `배틀자원`을 로드하고 자원 ID, 최대값, 초기값과 중첩 규칙을 검사한다.
4. `배틀스킬`을 로드하고 모든 ID가 `스킬`에 존재하는지 검사한다.
5. `배틀스킬효과`를 `스킬ID`로 그룹화하고 실행순서, 조건과 자원 참조를 검사한다.
6. `배틀스킬파생`의 부모와 파생 ID, 우선순위를 검사한다.
7. `클래스`의 스킬1~궁극기 참조가 모두 유효한지 검사한다.
8. 파생 관계에 순환 참조가 없는지 검사한다.
9. 검증이 모두 성공하면 불변 스냅샷으로 만들어 현재 캐시와 원자적으로 교체한다.
10. 실패하면 오류를 한글 로그로 남기고 마지막 정상 캐시를 계속 사용한다.

필수 검증 항목:

- 필수 셀 누락
- ID 중복 및 존재하지 않는 참조
- `스킬구분=파생`인데 부모스킬ID가 비어 있는 경우
- 일반·궁극기 스킬에 부모스킬ID가 들어간 경우
- 활성화된 배틀 스킬에 효과도 파생 규칙도 없는 경우
- 계수, 확률, 횟수, 지속턴이 허용 범위를 벗어난 경우
- 같은 스킬 안에서 실행순서가 중복된 경우
- 파생 스킬이 일반 행동 후보로 들어가는 경우
- 파생 관계의 순환 참조
- 효과와 파생 규칙이 존재하지 않는 자원·상태 ID를 참조하는 경우
- 조건 연산자와 조건값 타입이 맞지 않는 경우
- 자원의 초기값이 최대값을 초과하거나 상호배타 태세가 동시에 초기화되는 경우
- 같은 부모 스킬의 파생 규칙에서 우선순위가 충돌하는 경우

행 순서는 의미를 갖지 않으며 반드시 ID와 실행순서로 처리한다. 열 위치를 숫자로 고정하지 말고 헤더 이름으로 열을 매핑한다. 알 수 없는 헤더는 경고 후 무시할 수 있지만, 필수 헤더가 없으면 로드를 실패시킨다.

### 8.13 자동 행동 선택

스킬은 **조건식 AI와 행동 횟수 기반 쿨다운**으로 사용한다.

```text
클래스의 기본 스킬 ID 확인
→ 활성화된 배틀스킬 조회
→ 쿨다운·자원·HP·상태 조건 검사
→ 독립 행동이 아닌 파생 스킬 제외
→ 사용 가능한 후보 생성
→ 우선순위와 가중치로 행동 결정
→ 후보가 없으면 일반 공격
→ 선택한 스킬 효과를 실행순서대로 적용
→ 파생 규칙이 있으면 조건에 맞는 파생 스킬 실행 또는 예약
```

현재 시트의 `사용조건`, `행동분류`, `대상유형`은 기획자가 읽기 쉬운 문자열이다. 실제 구현 전 enum과 구조화된 조건 모델을 정해야 한다. 문자열을 코드에서 임의로 부분 일치시키는 구현은 피한다.

## 9. 전투 이벤트와 메시지

### 9.1 전투 이벤트 타입 초안

- `BattleStarted`
- `TurnStarted`
- `NormalAttackUsed`
- `SkillUsed`
- `AttackMissed`
- `AttackEvaded`
- `CriticalHit`
- `DamageDealt`
- `HealApplied`
- `ShieldApplied`
- `StatusApplied`
- `StatusRemoved`
- `CounterActivated`
- `CharacterDefeated`
- `BattleEnded`

### 9.2 메시지 생성 원칙

- 전투 계산과 메시지 생성을 분리한다.
- 전투 중에는 이미지와 애니메이션을 사용하지 않는다.
- 전투 이벤트 하나에 여러 메시지 템플릿을 둘 수 있다.
- 동일한 행동이 반복되어도 표현이 지나치게 반복되지 않게 한다.
- 공격 피해량, 회복량, 치명타, 상태 효과는 표시한다.
- 전투 중 정확한 현재 HP는 숨기고 HP 비율에 맞는 상태 문장을 표시한다.
- 전투 종료 시에는 남은 HP와 주요 통계를 공개한다.
- 초기 버전에서는 AI 생성 문장을 사용하지 않는다.

예시:

```text
{Actor}가 {Target}에게 일반 공격!
{Actor}의 공격이 {Target}을 향합니다!
{Actor}가 틈을 노려 공격합니다!
```

```text
치명타! {Damage}의 피해를 입혔습니다!
급소를 정확히 노렸습니다! {Damage}의 피해!
```

체력 상태 메시지 후보:

```text
{Target}은 아직 끄떡없습니다!
{Target}이 조금씩 밀리기 시작합니다.
{Target}의 상태가 심상치 않습니다.
{Target}은 간신히 버티고 있습니다.
```

체력 구간에 따라 메시지를 선택할 수 있다.

초기 메시지 템플릿 수 목표:

| 이벤트 | 최소 템플릿 수 |
| --- | ---: |
| 일반 공격 시작 | 5개 |
| 일반 피해 | 5개 |
| 치명타 | 5개 |
| 회피·실패 | 5개 |
| HP 상태 | 구간당 3개 |
| 승리·패배 | 각각 5개 |
| 클래스 스킬 | 스킬별 2~3개 |

## 10. 승패와 밸런스

### 10.1 승리 조건

- 상대 HP를 0 이하로 만들면 승리한다.
- 최대 주요 행동 16회에 도달하면 두 캐릭터의 남은 HP 비율을 비교한다.
- 남은 HP 비율 차이가 5%를 초과하면 비율이 높은 캐릭터가 승리한다.
- 남은 HP 비율 차이가 5% 이하이면 무승부로 처리한다.
- 동시 처치 가능 여부는 구체화가 필요하다.

### 10.2 목표

- 전투력이 높은 캐릭터가 분명히 유리해야 한다.
- 전투력만 보고 결과를 확정적으로 예상할 수 없어야 한다.
- 클래스와 스킬의 차이가 전투 로그와 결과에 나타나야 한다.
- 무작위성 때문에 능력치 의미가 사라지지 않아야 한다.
- 특정 클래스가 모든 상황에서 항상 우세하지 않아야 한다.

### 10.3 밸런스 검증

구현 시 실제 Discord에서 반복 실행하기보다 전투 시뮬레이터 테스트를 제공한다.

예상 검증 항목:

- 동일 조건 10,000회 실행 시 클래스별 승률
- 전투력 격차별 승률
- 평균 전투 턴과 소요 시간
- 스킬별 선택률과 기여도
- 무승부 비율
- 특정 스킬의 무한 반복 또는 교착 여부
- 극단적인 전투력·생활력·매력 입력

## 11. 캐릭터 소유권

랭킹 검색만으로는 해당 Discord 사용자가 실제 캐릭터 소유자인지 증명할 수 없다.

봇이 캐릭터의 실제 소유권을 확인할 방법은 없다. 장기적으로는 `게임 서버 + 캐릭터명`이 같은 캐릭터를 여러 사용자가 등록하지 못하게 할 예정이지만, 소유권 검증과 선점 분쟁 처리가 필요하므로 중복 등록 방지 구현은 후순위로 둔다.

초기 버전에서는 다음 원칙을 적용한다.

- 사용자당 등록 캐릭터는 하나다.
- 등록 정보는 모든 길드에서 공통으로 사용한다.
- 소유권을 인증하지 않는다.
- 서버 이전과 캐릭터명 변경을 자동으로 추적하지 않는다.
- 분쟁 대응과 운영진의 강제 등록 해제 기능은 추후 결정한다.

## 12. 도전 정책

배틀은 상대방의 수락 없이 즉시 시작한다.

- 도전 수락과 거절 기능을 제공하지 않는다.
- 수락 제한 시간과 반복 도전 쿨다운을 두지 않는다.
- 배틀 신청 차단 기능을 제공하지 않는다.
- 재대결 버튼을 제공하지 않는다.
- 도전자와 상대는 현재 같은 Discord 길드에 속해 있어야 한다.
- 길드당 게임 하나만 동작하므로 여러 배틀 또는 다른 놀이와 동시에 진행할 수 없다.
- 자기 자신과의 배틀 및 오프라인 사용자의 배틀 허용 여부는 추가 결정이 필요하다.

## 13. 저장 데이터 초안

정확한 DB 모델은 SQLite 도입 설계에서 확정한다.

### 13.1 `UserCharacter`

```text
GuildId
DiscordUserId
GameServer
CharacterName
ClassId
CombatPower
LifePower
CharmPower
LastSyncedAt
CreatedAt
UpdatedAt
```

### 13.2 `Battle`

```text
BattleId
GuildId
ChannelId
ThreadId
ChallengerUserId
OpponentUserId
WinnerUserId
ResultType
RandomSeed
BalanceVersion
StartedAt
EndedAt
```

### 13.3 `BattleParticipant`

```text
BattleId
DiscordUserId
GameServer
CharacterName
ClassId
CombatPowerSnapshot
LifePowerSnapshot
CharmPowerSnapshot
MaxHpSnapshot
OtherCalculatedStats
```

### 13.4 `BattleEvent`

모든 이벤트를 영구 저장할지는 결정이 필요하다.

```text
BattleId
Sequence
Turn
EventType
ActorUserId
TargetUserId
SkillId
Value
PayloadJson
RenderedMessage
CreatedAt
```

저장 공간과 조회 목적을 고려하여 다음 선택지를 검토한다.

- 결과와 요약만 영구 저장
- 주요 이벤트만 영구 저장
- 전체 이벤트를 일정 기간만 저장
- 난수 시드와 스냅샷만 저장하고 필요할 때 재생성

## 14. 재현성과 버전 관리

과거 전투 재현과 오류 분석을 위해 다음 정보를 저장한다.

- 난수 시드
- 참가자의 전투 시작 스냅샷
- 사용한 밸런스 데이터 버전
- 클래스·스킬 데이터 버전
- 전투 엔진 버전

밸런스나 Google Sheets 데이터가 변경되어도 과거 전투 결과 자체는 변경하지 않는다.

## 15. 오류와 복구

- 랭킹 수집 실패 시 마지막 정상 데이터를 사용할지 결정한다.
- 데이터가 너무 오래된 경우 배틀 시작을 막거나 경고한다.
- 스킬 데이터 로딩 실패가 전체 봇 시작을 막지 않게 한다.
- 전투 로그 전송 실패 시 재전송 또는 요약 결과 출력을 시도한다.
- 봇 재시작 시 진행 중 배틀은 초기 버전에서 취소 처리할 수 있다.
- DB 저장 실패 시 결과를 확정하기 전에 오류를 알린다.
- 전투 결과 저장과 전적 반영은 하나의 트랜잭션으로 처리한다.

## 16. MVP 제안

### 단계 1: 데이터와 전투 엔진

- 캐릭터 등록·조회·변경
- 랭킹 데이터 스냅샷 저장
- 클래스 데이터 로딩
- 전투력 기반 HP·공격력 계산
- 일반 공격
- 치명타와 회피
- HP와 승패
- 난수 시드 기반 전투 재현
- 전투 시뮬레이션 테스트

### 단계 2: Discord 배틀

- `/배틀 상대:@사용자`
- 명령 채널과 같은 길드 사용자 검증
- 명령 실행 채널에 전투 스레드 생성
- 시간차 전투 로그 출력
- 결과 요약
- 길드 단위 게임 잠금 관리

### 단계 3: 클래스 스킬

- Google Sheets의 `클래스`, `스킬`, `배틀스킬`, `배틀스킬효과`, `배틀스킬파생`, `배틀자원` 로더 구현
- 전체 시트의 참조 무결성 검증과 마지막 정상 캐시 유지
- 스킬과 효과 타입 파싱
- 파생 스킬의 즉시 실행, 반격 실행, 다음 행동 예약
- 스킬 선택 로직
- 쿨다운과 조건
- 버프·디버프·상태 이상
- 타입별 메시지 템플릿

### 단계 4: 기록

- 전적 저장
- 최근 배틀 조회
- 캐릭터별 승패
- 클래스별 통계

생활력과 매력의 전투 효과는 단계 1에서 확정하지 않아도 되며, 전투력과 스킬 기반 MVP가 안정된 뒤 추가하는 것을 우선 검토한다.

## 17. 구체화가 필요한 항목

### P0: 구현 전에 반드시 결정

1. **전투력 변환 공식**
   - HP·공격력·방어력 계산
   - 전투력 차이와 목표 승률
   - 극단적인 격차 처리

2. **선공 계산식**
   - 클래스별 선공 보정값
   - 무작위 보정 범위
   - 동률 처리

3. **남은 승패 예외 규칙**
   - 동시 처치 처리
   - 지속 피해로 처치한 경우의 승자
   - 전투 중 데이터 또는 메시지 전송 오류 처리

4. **남은 Discord 예외 정책**
   - 자기 자신과의 배틀 허용 여부
   - 오프라인 사용자와의 배틀 허용 여부
   - 랭킹 서비스 장애와 캐릭터 검색 결과 없음의 처리 차이

### 확정된 운영 정책

- 사용자당 캐릭터 하나, 봇 전체 공통 등록
- 랭킹 데이터 유효 시간 1시간
- 오래된 데이터는 배틀 직전 갱신하며 검색 실패 시 배틀 중단
- 상대방 수락, 제한 시간, 도전 쿨다운, 신청 차단, 재대결 버튼 없음
- 같은 Discord 길드의 사용자만 대상으로 지정 가능
- 명령 실행 채널에 공개 게임 스레드를 만들고 종료 후 잠금·보관
- 길드당 모든 게임을 합쳐 하나만 동시 실행
- 전투 시작 후 모든 행동 자동 처리, 중간 입력과 버튼 없음
- Discord 텍스트 메시지만 사용
- 선공 결정 후 교대 행동
- 기본 공격 100% 명중, 일반 회피 없음
- 피해량 90~110%, 치명타 10%·피해 배율 150%
- 조건식 AI와 행동 횟수 기반 쿨다운으로 스킬 선택
- 클래스는 `클래스` 시트에서 기본 스킬 5개와 궁극기 1개를 참조
- 원본 스킬 정보와 배틀 규칙을 별도 시트로 관리하고 동일한 스킬 ID로 연결
- 복합 효과는 `배틀스킬효과`에서 효과별 행으로 관리하고 실행순서대로 적용
- 파생 스킬은 독립 행동 후보에서 제외하고 `배틀스킬파생` 규칙으로만 실행
- 평균 주요 행동 8~12회, 최대 16회, 로그 간격 약 1.5초
- 최대 행동 도달 시 HP 비율 비교, 차이가 5% 이하면 무승부
- 전투 중 현재 HP는 상태 문장으로 표현하고 종료 시 수치 공개

### P1: 스킬 엔진 구현 전에 결정

1. `배틀스킬.행동분류`, `대상유형`, `사용조건`을 코드에서 사용할 enum과 조건 모델로 변환하는 방식
2. 스킬 우선순위와 선택 가중치의 범위 및 동률 처리
3. 기본 스킬 5개가 한 전투에서 사용되는 빈도와 일반 공격의 선택 조건
4. 스킬별 쿨다운과 최초 쿨다운 최종값
5. 버프·디버프 중첩, 갱신, 동시 적용 규칙
6. 상태 이상 저항과 해제 규칙
7. 다단 히트의 치명타·방어·확률 판정을 타격마다 수행할지 여부
8. 지속 피해와 회복의 적용 시점
9. 파생 스킬이 주요 행동 수와 행동권을 소비하는지 여부
10. 궁극기 게이지 획득 공식과 궁극기 사용 조건
11. 음유시인의 악상 선택, 중복 방지와 재추첨 규칙
12. 초 단위 원본 효과를 턴으로 변환하는 공통 기준
13. 스킬 및 메시지 데이터 버전 관리 방식
14. 클래스별 기본 공격 차별화 여부

### P2: MVP 이후 결정

1. 생활력의 전투 효과
2. 매력의 전투 효과
3. 전적 공개 범위
4. 랭킹과 시즌 도입 여부
5. 연승과 칭호
6. 관전자 응원 또는 개입
7. 토너먼트
8. 전투 로그 보관 기간
9. 놀이터 정리 및 보관 정책

## 18. 다음 기획 순서

다음 논의에서는 아래 순서로 구체화한다.

1. 전투력 기반 HP·공격력·방어력 변환 공식을 만든다.
2. 전투력 격차별 목표 승률을 정한다.
3. 선공 계산식과 동시 처치 규칙을 확정한다.
4. 확정된 기본 규칙으로 일반 공격 시뮬레이터를 만든다.
5. 목표 승률과 평균 행동 수를 기준으로 공식을 조정한다.
6. 현재 확정된 Google Sheets 6개 시트의 로더와 검증기를 구현한다.
7. 음유시인 스킬 11개와 효과·파생 규칙을 첫 번째 기준 데이터로 구현한다.
8. 음유시인 대 음유시인 시뮬레이션으로 효과 실행순서, 쿨다운, 파생 예약을 검증한다.
9. 음유시인 데이터의 계수와 턴 변환을 조정한다.
10. 전체 전투 로그를 출력하여 텍스트의 속도와 반복감을 검증한다.
11. 다음 클래스를 같은 스키마에 추가하여 클래스별 예외 코드 없이 확장되는지 확인한다.

## 19. 레퍼런스

### 19.1 Progress Quest

Progress Quest는 캐릭터 생성 이후 전투, 퀘스트, 전리품 판매와 성장까지 시스템이 자동으로 진행하는 제로 플레이어 RPG다. 공식 매뉴얼은 이를 `fire and forget` 방식으로 설명하며 플레이어가 전혀 상호작용할 필요가 없다고 명시한다.

몰리 배틀에서는 다음 원칙을 참고한다.

- 전투가 시작된 뒤 플레이어 입력을 요구하지 않는다.
- 캐릭터의 행동과 결과를 시스템이 모두 결정한다.
- 사용자는 계산을 조작하는 사람이 아니라 진행 상황을 관찰하는 사람이다.
- 내부 처리와 사용자에게 보여주는 진행을 분리한다.

출처:

- <https://progressquest.com/>
- <https://progressquest.com/info.php>

### 19.2 Godville

Godville은 영웅이 스스로 성장하고 모험하며 그 과정을 일기 형식으로 기록하는 텍스트 중심 제로 플레이어 게임이다. 정확한 계산식보다 캐릭터의 행동을 다양하고 유머러스한 문장으로 전달하는 데 집중한다.

몰리 배틀에서는 다음 원칙을 참고한다.

- 수치 계산보다 전투 상황을 설명하는 문장을 중심으로 보여준다.
- 같은 전투 이벤트에 여러 메시지 템플릿을 둔다.
- HP 상태와 위기, 역전, 승패를 이야기 흐름으로 표현한다.
- 사용자가 개입하지 않아도 다음 문장을 기다리게 만드는 중계 속도를 사용한다.

출처:

- <https://godvillegame.com/>
- <https://en.wikipedia.org/wiki/Godville>

### 19.3 Stone Story RPG

Stone Story RPG는 AI가 이동과 전투를 자동으로 처리하고 ASCII로 상황을 표현하는 오토 RPG다. 전투 중 사용자의 장비 교체와 능력 사용 개입이 가능하므로 몰리 배틀의 직접적인 사용자 경험 레퍼런스는 아니다. 다만 HP와 상태에 따라 행동을 선택하는 조건식 AI 구조는 참고한다.

몰리 배틀에서는 다음 원칙만 제한적으로 참고한다.

- 현재 HP와 상태 효과에 따른 행동 조건
- 사용 가능한 스킬 후보 선별
- 상황별 장비 대신 클래스 스킬의 우선순위 결정

출처:

- <https://store.steampowered.com/app/603390/Stone_Story_RPG/>

### 19.4 적용 범위

Progress Quest와 Godville은 무조작·텍스트 표현의 레퍼런스이며, 공정한 단판 PvP 밸런스를 직접 제공하지 않는다. 따라서 피해 공식, 전투력 격차별 승률, 최대 행동 수와 무승부 판정은 몰리 배틀의 목적에 맞게 별도로 설계하고 시뮬레이션으로 검증한다.
